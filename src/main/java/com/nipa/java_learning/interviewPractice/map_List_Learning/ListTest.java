package com.nipa.java_learning.interviewPractice.map_List_Learning;

import java.util.List;

/*
1)  public interface List<E> extends SequencedCollection<E> {
    //Means SequencedCollection interface is the parent interface of List interface
    }
2) public interface SequencedCollection<E> extends Collection<E> {
    //Means Collection interface is the parent of Collection interface
    }
3) public interface Collection<E> extends Iterable<E> {
    //Means Iterable is parent interface of  Collection interface
    }
4) public interface Iterable<T> {
    //Means Iterable is the root parent class of all child class of Collection
    }
 */

public class ListTest {


}
